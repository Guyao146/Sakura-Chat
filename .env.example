# 服务端口
PORT=3000

# JWT 签名密钥（生产环境务必改成一段长随机字符串）
JWT_SECRET=please-change-me-to-a-long-random-string

# 可选：启用 HTTPS/WSS（传输层加密）。把证书放到 server/data/ 下并取消注释：
# SSL_KEY_PATH=server/data/key.pem
# SSL_CERT_PATH=server/data/cert.pem
